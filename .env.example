# PDFasistant Environment Variables
# Copy this file to .env.local and fill in your API keys

# ============================================
# Primary AI Provider - Gemini (Required)
# ============================================
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ============================================
# Fallback AI Provider - Z.AI (GLM) (Optional)
# ============================================
# Required if using 'fallback' mode or 'glm' provider
# Get your API key from: https://z.ai/manage-apikey/apikey-list
# Documentation: https://docs.z.ai/api-reference/introduction
GLM_API_KEY=your_glm_api_key_here

# ============================================
# AI Provider Selection
# ============================================
# Options:
#   - 'gemini': Use only Gemini (requires GEMINI_API_KEY)
#   - 'glm': Use only GLM (requires GLM_API_KEY)
#   - 'fallback': Use Gemini as primary, GLM as fallback (requires both keys)
# Default: 'fallback'
AI_PROVIDER=fallback
